@{
    ViewBag.Title = ViewBag.Role + " Users";
}

<h2>@ViewBag.Title</h2>
<hr />
<table class="table table-hover">
    @foreach (var user in ViewBag.Users)
    {
        <tr>
            <td>
                @user.UserName
            </td>
        </tr>
    }
</table>